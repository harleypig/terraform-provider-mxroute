---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mxroute Provider"
description: |-
  A Terraform provider for MXroute email hosting.
---

# mxroute Provider

A [Terraform](https://www.terraform.io) provider for
[MXroute](https://mxroute.com) email hosting. It manages MXroute domains,
mailboxes, forwarders, pointers, and spam settings through the MXroute REST
API (`api.mxroute.com`).

## Requirements

- **Terraform >= 1.11.** The `mxroute_email_account` and `mxroute_reseller_user`
  resources use [write-only arguments][wo], which Terraform added in 1.11. The
  provider has no other version-gated features, so 1.11 is the baseline for the
  whole provider.

[wo]: https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments

## Example Usage

```terraform
terraform {
  # Write-only arguments (mxroute_email_account, mxroute_reseller_user)
  # require Terraform 1.11 or later.
  required_version = ">= 1.11"
}

provider "mxroute" {
  # All three may be omitted and read from the environment instead:
  #   MXROUTE_SERVER, MXROUTE_USERNAME, MXROUTE_API_KEY
  server   = "heracles.mxrouting.net"
  username = "myusername"
  api_key  = var.mxroute_api_key
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) API key, sent as `X-API-Key`. Falls back to the `MXROUTE_API_KEY` environment variable.
- `server` (String) MXroute mail server hostname, sent as `X-Server`. Falls back to the `MXROUTE_SERVER` environment variable.
- `username` (String) DirectAdmin username, sent as `X-Username`. Falls back to the `MXROUTE_USERNAME` environment variable.
